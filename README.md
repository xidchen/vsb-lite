# vsb-lite

The aim of this project is to burn subtitles into videos.

The default input subtitle directory is "subtitle_translated".

The default input video directory is "videos_no_sub".

The default output video directory is "videos_translated_sub".
